# inbox — 형식 자유 캡쳐 폴더

여기에는 **아무 형식으로나** 질문 메모(3~5줄)를 던진다. 질문 한 줄이어도 된다.

`/inbox` 스킬이 다음을 대신한다:
질문형 제목 확정 → frontmatter·태그 부여 → `## 메모`에 원본 보존 → `## 리서치`에 웹검색으로 답 보강(출처 병기)
→ `raw/qa/` 저장 → raw-map 갱신 → `## 관련 노트` 연결 → 처리한 원본 메모를 `old/`로 아카이브.

`README.md`와 `old/`를 제외한 모든 파일이 처리 대상이다.
